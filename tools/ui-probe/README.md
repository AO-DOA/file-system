# 几何探针（`tools/ui-probe/`）

回答一个问题：**顶栏（`.fs-hbar`）在哪些面板宽度上会把按钮裁掉或让两列叠在一起。**

做法是从 `src/client/index.tsx` 的 `const CSS = [...]` 逐字抠出样式，拼成一个静态 HTML（含
primitives 的真实 CSS module 规则 + 宿主壳层 CSS），在 headless Chrome 里把面板宽度从 1200 扫到
200，逐档读 `getBoundingClientRect` 与 `elementFromPoint`，把四项门禁读数打成表。

**脚本量的是工作树现状，不是 `client/client.js`**（见「前置条件」）。改完 CSS 不用 build 就能量。

---

## 1. 判据口径

这一节是口径的**正文**。`probe.js` 只是它的实现；两边改一处必须改另一处。四段 UI 改造期间所有
「0 档」「下界」的结论都是按这套口径读出来的（那段期间的「下界 356」与工作树现值「下界 339」用的是同一
口径），口径变了旧结论就作废。

### 1.1 可见矩形

对每个候选元素，先取它的**原始矩形** `raw = el.getBoundingClientRect()`，再取**可见矩形** `vis`：

```
vis = raw ∩ 每个 overflow != visible 的祖先的矩形（逐层求交，从近到远）
```

只算**祖先**的裁剪盒，不算兄弟、不算自身。任何一层 `overflow-x`/`overflow-y` 不是 `visible` 就参与
求交；求交结果为空时记成零矩形（`left=top=right=bottom=0`）。

为什么必须这样：`.fs-hbar` 自己带 `overflow:hidden`，右列溢出容器时元素的 `getBoundingClientRect`
**仍然是完整的**，只有算上祖先裁剪盒才看得见「它其实被切掉了」。直接读 `getBoundingClientRect`
会把最坏的那一档报成全绿。

### 1.2 四项门禁指标（只认这四项）

候选元素：`.fs-hbar-left` / `.fs-hbar-mid` / `.fs-hbar-right` 内的 `button`、`.fs-hd-path`、
`.fs-dirty`；被别的候选包住的元素会被剔除（只留最外层）。列归属按 `closest()` 判定，取 `L` / `M` / `R`。

**统计集合要与指标一起读**：上表四项**只统计这三类候选**，所以「被裁 N 档」的完整说法是
「**这三类候选**里有 N 档被整块裁掉」。注入进来的元素只有在自己也产出这些候选时才会进计数 ——
`--menus` 注进的面板是 `div.mr-list` / `div.mr-item`（探针的复刻 DOM），**一个 `button` 都不产出**，
所以四项读数与不带 `--menus` 时**逐档逐字段相同**（实测 3857 档 × 全部非 `menu` 字段 **0 差异**）。
反过来要记住：真实 `Menu` 的行是 `<button type="button" role="menuitem">`，**哪天把复刻行也改成真
`button`，打开态的面板自身就会落进「被裁」计数**（同一份源码的读数会突然翻倍，而那不是回归）。
指标口径变了旧结论就作废，统计集合变了同样是作废 —— 两件事都得写清。

| # | 指标 | 定义 | 门禁 |
|---|---|---|---|
| 1 | 跨列可见重叠 | 分属不同列的任意两个元素的 `vis` 相交面积 **> 0.5px²** ⇒ 该档记 1 次。另有一路「列盒子重叠」`colN`：三列的**原始矩形**两两相交 | 必须是 **0 档** |
| 2 | 同列可见重叠 | 同一列内任意两个元素的 `vis` 相交面积 > 0.5px² | 必须是 **0 档** |
| 3 | 被整块裁掉 | `raw` 有面积（>0）而 `vis` 面积 **≤ 0.5px²** ⇒ 整块不可见。另一路 `unclickN`：`vis` 面积 > 0.5 但 `elementFromPoint(vis 中心)` 命中的既不是它、也不与它互相包含 ⇒ 可见**但不可点** | 门禁看**第 4 项**；此项与 `unclickN` 用来定位成因 |
| 4 | 右列被整块裁 | 第 3 项里落在 `R` 列的那部分（`goneRightN`）。报的是「下界」——从 200 往右数，**右列第一次不再被整块裁**的面板宽度 | 下界 **≤ 360px**（工作树现值 **339**，余量 21px。基线 `b286bab` 是 307，中间那 389 是 R5 纯搬位置态 —— 三段数见本节的订正段） |

> **订正（2026-09-13）**：本表第 4 行原先把门禁依据写成「第三段 b 末态实测 356，余量仅 4px」。
> 356 是**那段当时的**读数（右列当时是独立的「编辑」+「保存」两个按钮；R4 分栏按钮先给右列
> +42px min-content，把它从 314 推到 356）。`8d5d591` 把编辑/保存**合并成一个**按钮之后，右列
> 最后一档被裁由 356 退到 **306** ⇒ **下界 307、余量 53px**；现在的门禁依据是这个数，产物
> `out-base-b286bab.json`（`--wsicon`，7 场景 × 551 档）与本 README §4 的同步前后对照表、
> `PROGRESS.md` §3 三处都是 307。所以「曾因分栏按钮退到 356」应读作**历史**（那段 b 的末态），
> 不是现值；`PROGRESS.md` §5 #11 的旧措辞已按此订正。
>
> **再订正（2026-09-13，R5 + 分栏让位档）**：**307 是基线 `b286bab` 的读数，不是工作树的现值。**
> 工作树源码经历了两次改动：R5 把视图选择器搬进右列（右列 min-content +74px）⇒ 纯搬位置态下界
> **389**（**破门禁**）；随后按用户裁决做窄档收纳（候选 B：窄档把「分栏」整块收起，阈值 620）
> ⇒ 下界 **339**、余量 21px。**现值是 339**（`out-final-lian3.json`）。三段读数与逐档 diff：

| 源码态 | 下界 | `gone` / `goneRight` / `partRight`（档数） | 产物 |
|---|---|---|---|
| 基线 `b286bab` | **307** | 760 / 504 / 667 | `out-base-b286bab.json` |
| R5 纯搬位置（破门禁） | **389** | 916 / 916 / 1045 | `out-after-lian2.json` |
| R5 + 分栏让位档（**工作树现值**） | **339** | 624 / 624 / 777 | `out-final-lian3.json` |

> 口径与 flags 三段完全一致（`--wsicon`，3857 档）。逐档 diff：搬位置→现值**变差 0 档 / 变好 1184 档**；
> 基线→现值**变差 509 档 / 变好 770 档**（变差的是 R5 搬动本身的残留代价，全部落在 210px 以上的档）。
> 细节见 `docs/agent/reports/2026-09-13-view-picker-to-right.md` §11。

「下界」为什么取最坏场景：同一个 `probe` 跑 7 个场景，每个场景各有一条下界，报数时取**最坏那条**
（S2/S6 的长名长路径最吃亏）。只报 S1 会系统性地乐观。

### 1.3 补充口径：右列部分裁切（**不在门禁内**）

`raw` 有面积、`vis` 面积 > 0.5、但 `vis` 的宽**小于** `raw` 的宽减 0.5 ⇒ 记为「部分裁切」
（`partRightN`，只统计 `R` 列）。含义是「看得见、点得到，但显示不全」。

它是**另一项指标**，不参与任何 pass/fail 判定。之所以单独列出来，是因为**只看第 3 项会漏掉它**：
第三段 a 末态在面板 499–534 有 17–36 档「右列部分裁切」（S1/S4 各 19 档、S2/S6 各 36 档），
第 3 项在这些档上是 0。门禁不认它，但它是回归信号——谁把它从 0 变回非 0 就是真退步。

### 1.4 其余读数

- `hbarH`：`.fs-hbar` 的**原始**高度。**单行断言 = 全部档的高度集合恒为 `{48}`**。折行会让它成倍
  增长，所以这是「顶栏有没有偷偷折成多行」的机器判据（第二段的「三块各占一行」候选方案就是这么被否的）。
- `wsLabShown` / `genLabShown`：工作区名 label、右列按钮 label 的 `offsetWidth > 0`，用来标定
  容器查询的**切换点**（实测 578/579 与 788/789 —— 后者所在宽段步长是 10，边界落在 780 与 790
  两个采样点之间）。
- **分栏让位档（620）没有专用字段**：它藏掉的是整个 `.fs-tipwrap` 锚点层，而候选元素表里最外层
  的 `button` 一旦 `display:none`，矩形全零 ⇒ 既不进「被整块裁」（那项要求 `raw` 有面积）也不进
  「部分裁切」。要标定它的切换点就用 `--dumpW`：**面板 648 档 `fs-splitbtn` 的 raw 宽是 0、
  649 档是 36.0 且与 vis 等宽**（`out-final-lian3-dump648.json` / `-dump649.json` 的 `dump` 字段）。
- `wsBtnW`：工作区按钮宽度。名字可见但按钮没变宽，通常说明 `max-width` 在钳制。
- `hbarOverflowRight`：`hbar` 右界 − (`fs-wrap` 右界 − 14)，右列溢出了多少。
- `--dumpW=<面板宽>`：只对那一档输出**逐元素**的 `raw` / `vis` / `clickable` / `hitTag` 与列盒子坐标，
  用来做单档取证（`hitTag` 是被 `elementFromPoint` 实际命中的元素，判「不可点是被谁挡的」靠它）。

### 1.5 菜单打开态（`--menus`，**交互态**场景）

**回答的问题**：下拉**打开时**面板到底看得见吗、被裁掉多少、在哪些面板宽度上整块不见。

顶栏的三个下拉（工作区 / 视图选择器 / 解读选择）是**交互态**：静态 DOM 里根本没有面板，所以
§1.2 的四项指标对它们一无所知。`--menus` 把「菜单已打开」注入场景，再用**同一套 `clipRect`**
（不新增口径）量面板自身：

- `--menus=inline`：面板留在锚点旁（`.mr` root 内，`.mr-list` 是 `position:absolute`）⇒ 它是
  `.fs-hbar` 的后代，**落进那条 `overflow:hidden` 的裁剪链** —— 这是修复前（`Menu` 未传
  `portal`）的真实形态。（`.fs-hbar-mid` 原先也有同样一条；视图选择器 2026-09-13 搬到右列后
  中列不再有按钮、那条裁剪已从源码删除，裁剪链如今只剩 `.fs-hbar` 一条。历史读数不受影响。）
- `--menus=portal`：面板搬到 `#stage` 之外（模拟 `createPortal(list, document.body)`；面板带
  `.mr-portal`，即 `position:fixed` + `z-index:1100`）⇒ 祖先链里没有 `.fs-hbar` —— 修复后的形态。
- `--menu=<ws|view|gen>`：只打开其中一个。真实使用**同一时刻只开一个**菜单；三个同时打开时
  portal 面板会互相遮挡（`hit=false`）是合成场景产物，不是缺陷。

逐档进 JSON 的 `menu` 字段（**只有带 `--menus` 时才有这个字段**）：

| 字段 | 含义 |
|---|---|
| `host` | **按祖先链实测**的承载形态：`hbar` = 面板仍在 `.fs-hbar` 子树里（会被裁）；`body` = 面板已挂到 `#stage` 之外。判据只看 DOM，不看 `--menus` 叫什么 |
| `visH` / `rawH` | 面板的可见高 / 原始高。`visH` 等于 `rawH` 就是一点没被裁 |
| `gone` | `vis` 面积 ≤ 0.5px² ⇒ **整块不可见**（「悬停看不到下拉」的字面读数） |
| `cropped` | `vis` 有面积但比 `raw` 矮 ⇒ 被切掉一截 |
| `hit` | 面板中心的 `elementFromPoint` 命中面板自身 ⇒ 可见且可交互 |

stdout 上多打一张专项表：面板 / 档数 / 承载形态 / 被整块裁 / 被裁（部分+整块）/ 最小可见高 /
面板高 / 中心不可命中，并逐面板列出**可见高集合**（去重）。

### 1.6 比两份产物之前先对齐 flags —— `--wsicon` 会整档改变「被裁」读数

`--wsicon` 不是显示细节：它决定工作区按钮的文字有没有 `.fs-wslabel` 这一层，而样式表的
`@container (max-width:760px){.fs-btnlabel:not(.fs-wslabel){display:none}}` 正是靠这个类名把
工作区名**排除在「收文字」之外**。所以同一份源码、同一台机器、前后脚两次运行：

| 运行（同一份**基线 `b286bab`** 源码） | 合计读数（3857 档） |
|---|---|
| 带 `--wsicon` | `gone 760` / `goneRight 504` / 右列最后被裁档 **306** |
| 不带 `--wsicon`（工作区名永不收，左列常宽） | `gone **1763**` / `goneRight **1507**` / 右列最后被裁档 **482** |

**同一组对照在工作树现值源码上（R5 + 分栏让位档）**：带 `--wsicon` 是 `gone 624` /
`goneRight 624` / 最后被裁档 **338**，不带 `--wsicon` 是 `gone **1571**` / `goneRight **1494**` /
最后被裁档 **514**（产物 `out-final-lian3.json` / `out-final-lian3-nowsicon.json`）。**两组都仍然
差着约 2.5 倍** —— 结论「漏 `--wsicon` 就足以让被裁读数翻倍」不随源码改版而变。

> 上表是 **`scenario()` 同步后**（2026-09-12）的读数；同步前的旧复刻是 `1010 / 754 / 356`（带
> `--wsicon`）与 `2013 / 1757 / 576`（不带），前后对照见 §4 —— 后面那组 2013 / 1757 正是下文事故里
> 被误读的数字。

判别特征：不带 `--wsicon` 时逐档 `wsLabShown` 恒为 `null`（`.fs-wslabel` 元素根本不存在）。

**纪律**：要对比的两份产物必须用**同一组 flags**。只差一个 `--wsicon` 就足以让「被裁」翻一倍，
看起来像一次惨烈回归 —— 2026-09-12 就有一份 `/tmp` 产物（`out-fix-cur.json`，`wsLabShown` 全 `null`）
的 2013 / 1757 被误读成「`--menus` 改变了汇总表口径」；把 flags 对齐后，`--menus` 的两份产物
与基线**逐档逐字段 0 差异**（实测，见 §1.2 与 §1.5）。至于历史源码（第三段 a 之前确实没有
`.fs-wslabel` 那一层），跑历史对照要按**那段源码**的真实形态选 flags —— §3 那条「否则探针会比
真实更容易通过」讲的就是这个反向坑。

---

## 2. 跑法

```bash
# 最少一次（默认 7 场景，步长 10 / 1，约 1 分钟）
node tools/ui-probe/probe.js --tag=now

# 带 DOM 变体与追加 CSS：不动源码先标定阈值
node tools/ui-probe/probe.js --tag=var550 --wsicon --extra=/tmp/extra.css

# 分栏开启态
node tools/ui-probe/probe.js --tag=sp --splitpane

# 菜单打开态：修复前（内联，面板被顶栏的 overflow 裁掉）与修复后（portal）各跑一遍
node tools/ui-probe/probe.js --tag=m-inline --wsicon --menus=inline
node tools/ui-probe/probe.js --tag=m-portal --wsicon --menus=portal

# 只打开视图选择器（三个 portal 面板同时开会互相遮挡，测单开用它）
node tools/ui-probe/probe.js --tag=m-view --wsicon --menus=portal --menu=view

# 单档取证
node tools/ui-probe/probe.js --tag=d400 --dumpW=400
```

### 2.1 具名选项

| 选项 | 默认 | 作用 |
|---|---|---|
| `--tag=<名>` | `probe` | 产物文件名的一部分 |
| `--css=<tsx 文件>` | `<仓库根>/src/client/index.tsx` | 从哪个文件抠 CSS 数组（用来对比历史快照） |
| `--wsicon` | 关 | 工作区按钮文字**包进** `.fs-wslabel`（第三段 a 之后的真实形态）。**它参与决定读数**：漏了它「被裁」会从 1010 档跳到 2013 档（见 §1.6），比产物之前先对齐这个 flag |
| `--splitpane` | 关 | body 里多一条分隔条 + 一份只读副本窗格（R4 分栏开启态） |
| `--menus=<inline\|portal>` | 关 | 注入**菜单打开态**：`inline` = 面板留在锚点旁（修复前，会被顶栏的 `overflow:hidden` 裁）；`portal` = 面板挂到 `#stage` 之外（修复后）。裸 `--menus` 等同 `inline`。产物文件名会带上形态后缀 |
| `--menu=<ws\|view\|gen>` | 三个全开 | 只打开其中一个菜单（配合 `--menus`）。真实使用同一时刻只开一个，测单开用它 |
| `--extra=<css 文件>` | 无 | 追加一段 CSS（标定阈值用，不改源码） |
| `--only6` | 关 | 只跑 S0–S5，去掉 S6 |
| `--sn=<n>` | 1 | 窄段（700→200）步长 |
| `--sw=<n>` | 10 | 宽段（1200→701）步长 |
| `--dumpW=<宽>` | 0 | 只对给定宽度输出逐元素 dump |
| `--shell-css=<dir>` | 自动推导 | 壳层 CSS 目录 |
| `--outdir=<dir>` | `$TMPDIR/dsh-ui-probe` | DOM 与 JSON 的落盘目录 |
| `--chrome=<路径>` | `/usr/bin/google-chrome` | headless Chrome 可执行文件 |
| `--window=<W,H>` | `1600,1000` | Chrome 窗口尺寸。**必须是逗号分隔**（见 §2.2 第 2 条） |

### 2.2 三个坑（都踩过）

1. **位置参数被静默忽略**。参数解析只认 `--名` / `--名=值` 两种形态，别的字符串直接丢掉，**不报错**。
   `node probe.js now` 不会报错，只会用默认 `--tag=probe` 覆盖上一轮的产物，让人以为「跑了新变体、
   结果没变」。跑变体时**必须**写 `--tag=`，并且回看 stdout 里打印的 JSON 路径确认 tag 对不对。
2. **`--window-size` 必须够大，而且格式必须是 `W,H`**。窗口比元素窄时元素落在视口外，
   `elementFromPoint` 拿不到命中 ⇒ 第 3 项的 `unclickN` 出现**假红**。默认 `1600,1000` 是照最宽
   采样档 1200 留的余量；把 `--sn/--sw` 往高调、或把面板宽度加上去，就得同步调 `--window`。
   **格式坑（本仓实测）**：Chrome 的 `--window-size` **只认逗号**，写成 `1600x1000` 会被**静默忽略**
   并退回默认窗口——症状是面板 830–1200 档全体误报「不可点，`hitTag=NULL`」，而矩形、重叠、裁剪、
   顶栏高度这四类读数**完全正常**（它们不看视口）。脚本已把 `x`/`X`/`*` 归一成逗号，但自己传参时
   仍要记住这条：**只有 `unclickN` 一项集体变红时，先怀疑窗口尺寸**。
3. **`overflow` 判据用的是计算值，不是原始值**。`overflow-x` 与 `overflow-y` 只要有一个不是
   `visible` 就参与裁剪（CSS 的 `visible`+非 `visible` 组合会互相提级，这里的宽口径只会更严，不会漏）。

### 2.3 环境依赖

- **headless Chrome**：`/usr/bin/google-chrome`，用 `--headless=new --dump-dom` 出结果，不需要
  puppeteer/playwright，也不新增任何 npm 依赖。
- **壳层 CSS 变量**：来自 **DSH 检出的构建产物** `apps/web/dist/assets/*.css`（默认按
  `<仓库根>/../../../deepseekHARNESS/apps/web/dist/assets` 推导）。探针按文件名全量 `<link>` 进来，
  所以量出来的字号、行高、间距与浏览器里看到的一致。**宿主没 build 过就是空的**，这时四种指标的
  绝对值都不作数——`apps/web/dist/assets` 不存在时脚本会直接报错并提示，不会静默降级。
- **primitives**：`node_modules/@deepseek-ai/dsh-client-ui-primitives/lib` 下的 `Button.module.css` /
  `Menu.module.css` 被读进来，类名换成无 hash 前缀（`vp-*` / `mr-*`）。映射表是**白名单**，
  上游新增类名会 `throw`（宁可报错，不要悄悄少一条规则）。
- **`require`**：仓库根是 `"type": "module"`，本目录的 `package.json` 就地声明 `"type": "commonjs"`，
  探针才能用 `require` / `__dirname`。
- **lint 覆盖范围**：`.oxlintrc.json` 的 `ignorePatterns` 里**没有** `tools/**`，所以 `probe.js`
  会被 `npm run lint` 扫到，改它要保持 **0 错 0 警告**（`scripts/**` 相反被显式忽略）。
- **不进发布包**：根 `package.json` 的 `files` 不含 `tools/`，探针不随插件发布，纯开发工具。

---

## 3. 场景定义

`fs-hbar` 是三列网格（左：工作区按钮 + 两个图标按钮；中：**只有路径**；右：视图选择 / 解读选择 /
未保存 / 编辑⇄保存（**合并成一个**按钮）/ 分栏 —— 右列次序即此，视图选择器于 2026-09-13 按用户裁决
从中列搬来）。七个场景覆盖三列的各种「内容最宽」组合：

| id | 工作区名 | 中列 | 右列 | 用途 |
|---|---|---|---|---|
| `S0-empty` | 中（`dsh-plugin-file-system`） | 无 | 只有分栏按钮（没有打开对象 ⇒ 视图选择器不渲染） | 最空形态；基线在面板 200–209 有 10 档右列部分裁切 —— 分栏让位档（620）落地后**归零**（那 10 档整个按钮都收起了） |
| `S1-source` | 中 | 中路径 | 视图「源码」+ 解读选择 + 未保存 + 保存（合并按钮） | 主力场景 |
| `S2-trlong` | **长** | 长路径 | 视图「文章翻译」+ 重新翻译 + 未保存 + 保存（合并按钮） | 最坏场景之一，下界通常由它决定 |
| `S3-dir` | 中 | 中路径 | 视图「目录概览」+ 解读选择 | 右列最简，用来对照 |
| `S4-trbusy` | 中 | 长路径 | 视图「源码」+ 翻译中…（**禁用态**）+ 未保存 + 保存（合并按钮） | 禁用按钮的几何与可点判定 |
| `S5-short` | 短（`dsh-fs`） | 短路径 | 视图「源码」+ 解读选择 + 编辑（合并按钮，无 `dirty`） | 其它维度的最短组合 |
| `S6-wsxlong` | **超长**（89 字符） | 同 S2 | 同 S2 | 补实测：超长名不会进一步抬高下界（`max-width:220px` 钳制） |

- 路径三档：`src/index.tsx`（短）/ `dsh-plugin-file-system/src/client/index.tsx`（中）/
  `deepseekHARNESS/packages/client/ui-primitives/src/components/very/deep/nested/folder/with-a-really-long-file-name.module.tsx`（长）。
- **S6 的名称长度按实测记：`String.length === 89`**。第三段 a 报告与派单书写「79 字符」，与实测
  不符（差 10），此处按实测值记；两个数的口径差异未核实（不排除当时只数了前半段）。
- `--wsicon` 决定工作区按钮的文字是否包进 `.fs-wslabel` 层。第三段 a 之前的源码**没有**这一层，
  用当前源码跑历史对照时必须知道这一点，否则探针会比真实更容易通过。
- **`.fs-tipwrap` 锚点层必须复刻**（2026-09-12 已同步）：源码里每个挂了 `Tooltip` 气泡的顶栏按钮
  外面都包一层原生 `span.fs-tipwrap`（`tip()` —— primitives 的 `Button` 是普通函数组件，React 18 下
  挂不上 ref，气泡锚点只能是原生元素）。探针复刻 **5 处**：工作区、左列两个图标按钮、分栏、编辑⇄保存。
  **视图选择器与解读选择不带这一层**：它们「悬停即开下拉」，不挂气泡是刻意的例外（`tip()` 旁边的
  注释说明了理由）。少复刻一层，量的就是另一个 DOM（这一层实测几何中性，证据见 §4）。
- **分栏按钮必须带 `fs-splitbtn` 类名**（2026-09-13 已同步）：窄档那条
  `@container (max-width:620px){.fs-hbar-right > .fs-tipwrap:has(.fs-splitbtn){display:none}}`
  拿它当锚点，从按钮找到外层的 `.fs-tipwrap` 一起藏。复刻少了这个类名，规则在探针里**匹配不到
  任何元素** —— 量到的会是「没做收纳」的另一个 DOM（下界停在 389，看起来像收纳失效）。
- **右列只有一个「编辑⇄保存」按钮**（2026-09-12 已同步）：判据是 `canSave = editMode || dirty`，
  同一节点换态，复刻里不再是「编辑 + 保存」两个。场景数据用 `editLabel` 标出该场景显示的那一态
  （有 `dirty` 的场景是「保存」）—— 两种文案都是两个汉字 + 16px 图标，几何等价。
- 视图选择器的文字是**裸文本**（源码就是这么写的，没有 label 层），探针必须保持一致。
- **菜单打开态不是第 8 个场景**，而是给上面这 7 个场景加的一个开关（`--menus`）：面板插在**真实锚点**
  的位置上（工作区 / 视图选择器 / 解读选择），分别复刻内联与 portal 两种承载形态。面板内容取
  locale 的真实文案、行数与真实菜单一致（工作区 1 行 / 视图 3 行 / 解读 4 行）；它的**绝对高度**
  （64 / 176 / 232）只用来比较，结论「被裁 / 不被裁」不依赖它。三个面板同时打开时 portal 面板会
  互相遮挡（`view` 中心 `hit=false`）—— 这是合成场景的产物，**用 `--menu=view` 单开即可消除**。
- 注入打开态**不改变**顶栏 DOM，所以四项门禁读数应与不带 `--menus` 时逐档逐字段一致（实测
  3857 档 × 全部非 `menu` 字段 **0 差异**）—— 这条可以当自检用。**自检的成立条件是两份产物用同一组
  flags**（`--wsicon` 必须一致，否则「被裁」会从 1010 跳到 2013，见 §1.6）。

---

## 4. 前置条件与局限

- **探针只回答你让它问的问题 —— 交互态缺陷必须固化成新场景（用法纪律）**。场景是静态 DOM 复刻
  （见本节第 4 条），看不见浮层、开关、异步这些**交互态**。本仓实测过这条的代价 —— 3857 档四项指标
  **全绿**的同时，页面上「悬停看不到下拉」：三个 `Menu` 默认内联渲染，面板是 `position:absolute`，
  而 `.fs-hbar` 带 `overflow:hidden`（为消跨列重叠加的兜底裁剪；当时 `.fs-hbar-mid` 也有一条，
  2026-09-13 随视图选择器搬走而删除），面板作为它的
  后代被**整块裁光**（视图选择器 3306/3306 档可见高恒 0）。**纪律**：任何交互态缺陷一旦被发现，
  当场固化成探针的一个新场景 / 新形态（`--menus` 就是这么来的，见 §1.5），否则同一类缺陷下一次
  仍会全绿溜过去。
- **量的是工作树现状，不是运行中的产物**：CSS 从 `src/client/index.tsx` 的 `const CSS = [...]`
  里逐字解析（`].join('\n')` 之前的那一段，每个元素一条单引号字符串字面量）。改了源码不 build，
  探针立刻能看到新样式；反过来，**它看不到 `client/client.js` 里的旧样式**。要对照「运行中是什么样」，
  得用 `--css=<历史快照>` 指过去（例：`/tmp/stage3/stage2-end.tsx`）。
- CSS 数组的边界是字符串匹配（`const CSS = [` 与 `].join('\\n')`）。改这两个锚点会让探针报错，
  这是有意的——静默抠到半截比报错糟。
- 探针是**静态 DOM 复刻**，不是渲染真组件：它不跑 React、不跑 locale、不跑状态机。几何受 DOM 结构
  与类名影响，源码改了结构（比如按钮换层）必须同步改 `scenario()`，否则量的是另一个东西。
- **复刻 DOM 的同步日期（2026-09-12，工作树，落在上一段末态提交 `8d5d591` 之后）**：`scenario()` 已与
  `src/client/index.tsx` 的真实渲染结构对齐 —— ① 补上 5 处 `.fs-tipwrap` 锚点层（工作区 / 左列两个
  图标按钮 / 分栏 / 编辑⇄保存）；② 右列的「编辑 + 保存」纠正为**一个**合并按钮。**同步前后各跑一遍
  基线口径**（`--wsicon`，7 场景 × 551 档 = 3857 档）：

  | 读数 | 同步前（旧复刻） | 同步后（与源码一致） |
  |---|---|---|
  | 跨列可见重叠 / 同列可见重叠 | 0 档 / 0 档 | 0 档 / 0 档 |
  | 被裁合计（`gone`） | 1010 档 | **760 档** |
  | 右列被整块裁 | 754 档，最后被裁档 356（下界 **357**） | 504 档，最后被裁档 306（下界 **307**） |
  | 右列部分裁切（`partRight`） | 886 档 | 667 档 |
  | 顶栏高度集合 / S0 被裁 | {48} / 0 档 | {48} / 0 档 |

  逐档 diff：957 档有差异，**全部是计数减少**（变差 0 档）。两处改动另外做了**单变量对照**：
  「只加 `.fs-tipwrap` 层（右列仍是两个按钮）」与「只合并按钮（锚点层替换成恒等）」各自与对应基线
  **逐档逐字段 0 差异** ⇒ 锚点层几何中性，读数变化只来自「右列少一个按钮」。
  **与上一段报告的差异**：那段的外挂装置（未入库的 `/tmp/e2/measure-new-dom.mjs`）记 `gone` **790**，
  本轮复跑得 **760**（差 30 档，占 3857 档的 0.8%）；结构性指标（下界 307、`goneRight` 504、跨列/同列
  0 档、`hbarH` {48}）与那段一致。差异来源**未定位**（该装置不进仓库，其 DOM 变体细节无法逐字核对）；
  本 README 以本仓产物 `out-sync-after.json` 的读数为准。**以后读数变了，先看这一段判断是源码变了、
  还是复刻又滞后了。**
- **复刻 DOM 的第二次同步（2026-09-13，工作树）**：用户裁决把视图选择器从中列搬到右列，`scenario()`
  随之同步 —— ③ 中列去掉视图选择器（只剩路径）、右列加入它并把「分栏」退到末尾，右列次序＝源码
  JSX 次序（视图选择 → 解读选择 → 编辑⇄保存 → 分栏）。**同步与源码改动是同一件事**：不同步就是在量
  另一个 DOM。同步后 `--wsicon` 全扫（3857 档）读数：跨列 0 / 同列 0 / 顶栏高 `{48}` 不变，被裁合计
  760 → **916 档**、右列被整块裁 504 → **916 档**、右列整块裁下界 **307 → 389**（**破门禁 ≤360**）；
  逐档 diff 444 档有差异、**全部是变差（0 档变好）**。产物 `out-after-lian2.json`，候选收纳方案的
  实测对照见 `docs/agent/reports/2026-09-13-view-picker-to-right.md`。
- **复刻 DOM 的第三次同步（2026-09-13，工作树，同一单的后半）**：收纳裁决（候选 B）落地时，
  `scenario()` 的分栏按钮补上 `fs-splitbtn` 类名（与源码同一个）。**同步与源码改动仍是同一件事**：
  少这个类名，620 档那条规则在探针里匹配不到元素，量到的就是「没做收纳」的 DOM。同步后 `--wsicon`
  全扫（3857 档）读数：跨列 0 / 同列 0 / 顶栏高 `{48}` 三项不变，被裁合计 916 → **624 档**、
  右列被整块裁 916 → **624 档**、右列整块裁下界 **389 → 339**（回到门禁内，余量 21px）；相对第二次
  同步的产物逐档 diff：**变差 0 档、变好 1184 档**。产物 `out-final-lian3.json`（分屏态
  `out-final-lian3-split.json` 与它**逐档逐字段 0 差异**）。既有两档的切换点未受影响（`wsLabShown`
  578/579、`genLabShown` 780/790 三份产物逐档相同）—— 「这条隐藏规则不与既有两档打架」就是靠这个
  实测判定的。
- 绝对像素端点会随**字体渲染**小幅浮动（本机 Linux Chrome）。所以阈值一律按约 **8% 余量**取整
  （672 / 470→550 / 760 / 620 四处同一口径），换字体族必须复测切换点。
- **跨运行有可测的非确定性**：同一份 CSS、同一个脚本、同一台机器，两次运行之间也会有个位数的档
  对不上。实测标定（`--wsicon` 全扫 3857 档 × 14 个字段）：原版脚本「历史那次 vs 现在这次」有
  **60 处**字段差异（`genLabShown` 48、`goneN` / `goneMidN` / `partRightN` 各 4），全部落在档界附近。
  **推论**：判回归看**结构性指标**（「0 档」「下界」「顶栏高集合」），不要拿单次运行里 1–2 档的差异
  当回归；真要坐实某个差异，同条件复跑两次再下结论。
- 本脚本已按上面这条做过等价性验证：仓库版与 `/tmp` 原版在**同一次环境**里跑，14 个字段 × 3857 档
  **完全一致**（0 差异）。

---

## 5. 产物

都在 `--outdir`（默认 `$TMPDIR/dsh-ui-probe/`）下，**不进仓库**：

- `probe-<tag>.html`：拼好的静态页，约 30KB。可以直接用浏览器打开、改窗口宽度肉眼对照。
- `out-<tag>.json`：逐档读数，单份 **约 1MB**（7 场景 × 几百档）。这就是「可复跑的产物文件」，
  台账与报告里的数字要引用它，不要引用转述。**带 `--menus` 时文件名会追加 `-inline` / `-portal`
  后缀**（`out-<tag>-<形态>.json`），免得两个形态互相覆盖。

stdout 上打两张表：逐场景汇总（档数 / 跨列 / 同列 / 被裁 / 右列被裁 / 顶栏高集合）与合计；
带 `--menus` 时前面多一张菜单打开态专项表（§1.5）。

---

## 6. 差异

判据实现（`clipRect` / 面积求交 / 四项汇总）**逐字未改**，只改了环境耦合处：

| 处 | 原版 | 本版 | 为什么 |
|---|---|---|---|
| 仓库根 | 硬编码绝对路径 | `path.resolve(__dirname, '..', '..')` | 换机 / 换检出位置 |
| 壳层 CSS 目录 | 硬编码 | 按布局推导 + `--shell-css` 覆盖 | 同上 |
| 输出目录 | 硬编码 `/tmp/stage3/` | `--outdir`，默认 `$TMPDIR/dsh-ui-probe` | 别把运行产物写进别人的工作目录 |
| Chrome / 窗口 | 硬编码 | `--chrome` / `--window` | 换环境 |
| 依赖缺失 | 裸 ENOENT | 前置校验 + 中文提示 | 报错要能指导下一步 |
| 头部注释 | 面向当段任务 | 面向可复用（指向本 README） | 口径的正文只有一份 |

第二笔来自 `/tmp/menu-probe/menu-probe.js`（「解读选择胶囊 图层错误」诊断时的**复制变体**，
由 `/tmp/patch-menu-probe.py` 从本文件生成）。它注入的**菜单打开态**已经**合并进本文件**成具名选项
`--menus` / `--menu`（见 §1.5），变体本身不再维护、也不在仓库里留副本：

| 处 | 变体 | 本版 | 为什么 |
|---|---|---|---|
| 开关 | `--mode=inline\|portal` | `--menus=inline\|portal`（裸 `--menus` = inline） | 名字说明「开的是菜单」 |
| portal 承载 | 面板塞在 `.fs-wrap` 之后的 `.fs-portal` 容器里 | 面板挂到 `#stage` **之外**的 `#portal`（body 直下），并带 `.mr-portal`（用 Menu.module.css 的 `.portal` 真规则，而不是手写 `style.position`） | 与 `createPortal(list, document.body)` 的祖先链一致：链路里没有 `.fs-hbar` |
| 形态判据 | 只看 `--mode` 的名字 | 逐档读 `host`（**按 `closest('.fs-hbar')` 实测**祖先链） | 结论来自 DOM，不来自选项名 |
| 面板读数 | 直接塞进每档记录 | 只在带 `--menus` 时写 `menu` 字段 | 不带该选项时产物与旧版逐字段一致，历史 JSON 还能对 |

`/tmp` 下的旧版（`gen.js` / `gen3.js` / `gen4.js` / `menu-probe.js`）不再维护。**新的几何问题一律改这里**，
别再往 `/tmp` 复制一份新的——第三段期间同一脚本被从头复活过三次，就是为了这个教训。
