---
源码路径: ${srcKey}
层级: 文件
生成时间: ${generatedAt}
---

# ${fileName}

**路径**：${srcKey}

> **解决什么问题**：<一句话：这个文件存在的价值，写给不懂项目的人>

## 核心概念（这个文件只做一件事）

<一句话主题 + 2-4 句展开：概念怎么运作；关键术语在这里自然解释，**不单独列术语表**>

## 主线走查（按调用/依赖顺序）

### 1. `<导出名>`（<被谁用>）
- **做什么**：<职责，引用真实签名/字段>
- **为什么这么设计**：<JSDoc/注释里的动机；没有就写"推测：……">

### 2. `<导出名>`（...）

（……按主线排完所有关键导出）

## 在整体中的位置

**谁用它（被引用）**：
- `<模块>`, `<模块>` — 调用本文件导出（grep 实际结果）

**它用谁（引用）**：
- `<模块>` — <作用>

## 导出接口（附录：速查表）

| 导出 | 类型 | 作用 |
|---|---|---|
| `<导出名>()` | 函数 | <一行职责> |
| `<常量名>` | 常量 | <一行职责> |
