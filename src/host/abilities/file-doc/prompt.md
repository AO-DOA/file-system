任务：为【${target}】这一个文件写文件层（L2）文档，产物写入【${docPath}】。

【模式】${mode}
${modeHint}

【边界】
只生成这一篇；绝不为其它文件/文件夹生成第二篇；不递归、不批量。

【固定参数（原样使用，禁止改写，禁止按当前目录自动推导）】
目标文件 TARGET = ${target}
产物文档 DOC = ${docPath}
源码路径键 KEY = ${targetKey}

【骨架原文（宿主已渲染好，直接照用）】
===== 骨架开始 =====
${skeleton}
===== 骨架结束 =====
骨架里的 frontmatter 三行（源码路径 / 层级 / 生成时间）、`# <文件名，含扩展名>` 标题、`**路径**：…` 行、四个章节标题（`## 核心概念（这个文件只做一件事）` / `## 主线走查（按调用/依赖顺序）` / `## 在整体中的位置` / `## 导出接口（附录：速查表）`）、导出表头 `| 导出 | 类型 | 作用 |`，全部是宿主写好的确定值；最终写入 DOC 的文档必须逐字保留它们。

【步骤】
1. 通读骨架，分清哪些是确定值：frontmatter 三行、标题、路径行、四个章节标题、导出表头。
2. 仅当【模式】为「更新」时：先读现有文档 DOC，记下已写明的职责与措辞作为基线，保留其中仍然正确的部分。这一步必须在写 DOC 之前完成——写入是整篇覆盖，会把它冲掉。
3. 通读目标文件 TARGET，弄清它到底做什么；大文件分段读，超过 800 行不要一次读完。
4. 用检索工具（glob/grep）查引用关系：谁引用了本文件的导出（grep 导出名）、本文件 import 了哪些模块；结果用于「在整体中的位置」，不许凭猜测写。
5. 用写文件工具把「骨架原文 + 五段正文」整篇写入 DOC（UTF-8）。
6. 按【验收】逐条核对。

【正文结构（按模板结构写，五段）】
解决什么问题 → 核心概念一句话 → 主线走查 → 在整体中的位置 → 导出接口速查附录。
- 核心概念必须是一句话本质；主线顺序=依赖顺序（被依赖的先讲）；每个主线条目写「为什么这么设计」（真实动机优先，没有则写「推测：…」）；
- 术语在主线中自然解释，不单列术语表；导出接口表逐个核对真实代码，不凭名字猜；
- 全简体中文，代码/路径/专有名词保留原文；不编造。

【验收】
DOC 存在；frontmatter 含「源码路径: ${targetKey}」「层级: ${layer}」「生成时间」三行，且与骨架逐字一致；
正文五段结构齐备；文档中无 `<...>` 占位残留；全程只产出这一篇。

【已知坑，务必规避】
- 不要手写或改写 frontmatter（含「生成时间」）。
- 不要写 index.json（宿主负责）。
- 骨架里的占位符必须替换干净，交付文档中不得残留占位字样。
- md/markdown 文件宿主已拒绝生成 L2，若被要求也不要处理。
- 本层没有技能与 shell 工具，不要尝试调用任何技能或脚本。
- **每次回答必须先发工具调用再写解释**：长篇推理会挤掉输出预算，造成空转（无工具调用却结束）。

【问题台账（仅本轮确实出问题时才写，由你本人建档）】
若本轮出现返工 / 报错 / 产出不符预期 / 路径失效 / 判断失误 / 流程可优化之处，你要自己把这一条记下来——你是执行者，只有你知道现场发生了什么：
- 文件路径：${issueDir}/${issueDate}-${issueNo}-<slug>.md
  其中 `<slug>` 换成 3~5 个英文小写词、用连字符连接（例：l2-placeholder-left）。
- 文件内容按下列结构写（照抄结构，`type` 按实际情况替换）：

  ---
  date: ${issueDate}
  status: 问题提交
  skill: file-doc
  type: 流程不畅
  severity: 中
  recurrence: 1
  related: ""
  ---

  # <一句话问题标题>

  ## 场景（怎么触发）
  走到哪一步、读了什么、复现条件。

  ## 现象（看到了什么）
  报错文本 / 错误产出 / 需要返工的步骤，尽量贴原文。

  ## 根因（为什么会这样）
  为什么出问题：骨架填充规则、流程约定、还是路径假设有漏洞。

  ## 改法 / 临时绕过
  已做的修正，或本次手动绕过的办法。

  ## 复验（如何确认已解决）
  确认「这次不会再犯」的核验方式。

- 本轮一切顺利时，**不要**写任何台账文件。

【汇报（只说这些）】
- 顺利：只回复一句「本次任务完美执行」，不要列模式、路径、步骤或清单。
- 有问题：写出台账文件名（如 ${issueDate}-${issueNo}-<slug>.md）+ 一句现象，其余不要展开。
