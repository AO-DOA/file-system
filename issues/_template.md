---
date: YYYY-MM-DD
status: 问题提交
skill: source-doc
type: 脚本bug
severity: 中
recurrence: 1
related: ""
---

# <一句话问题标题>

> 本模板：复制为 `issues/<date>-<序号>-<slug>.md`，删掉本行。每次使用技能时若遇到**返工 / 报错 / 产出不符预期 / 路径失效 / 判断失误**，就把这一条记下来。**只记"哪里不好使、为什么"，不写技能能做什么**。

## 场景（怎么触发）

描述这次使用技能时走到哪一步、用了什么命令/参数。写清复现条件。
例：`node scripts/source-annotate.mjs build 骨架.md --src src/x.js --rel A/src/x.js --pkg A --book .book/A-book`

## 现象（看到了什么）

报错文本 / 错误的产出 / 需要返工的步骤。尽量贴原文。

## 根因（为什么会这样）

分析为什么会出问题——是脚本判断规则、模型流程约定、还是环境路径假设有漏洞。

## 改法 / 临时绕过

已做的修正，或这次使用中手动绕过的办法。

## 复验（如何确认已解决）

确认"这次不会再犯"的核验方式。**状态**==`已解决`/`已验证关闭` 前必须能回答这里。

---
# 状态机流转（每条演进时更新 `status` 与 `recurrence`）

- `问题提交`（首次记录）→ 分析根因后 `分析中` → 修复后 `已解决` → 复验通过 `已验证关闭`
- 同类问题**再次触发**：`recurrence` +1 —— 高重复且未 "已验证关闭" 的问题才是真正需要驱动技能进化、值得改 SKILL.md 的
- 状态与 recurrence 的演进也同步更新 `issues/README.md` 的索引表
